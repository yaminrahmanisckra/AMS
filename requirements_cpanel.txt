# Academic Management System - cPanel Requirements
# Compatible with Python 3.9.22 on shared hosting

# Core Flask dependencies
Flask==2.3.3
Werkzeug==2.3.7
Jinja2==3.1.2
MarkupSafe==2.1.3
itsdangerous==2.1.2
click==8.1.7

# Database
Flask-SQLAlchemy==3.0.5
SQLAlchemy==2.0.21
alembic==1.12.0

# Authentication & Security
Flask-Login==0.6.3
Flask-WTF==1.1.1
WTForms==3.0.1
email-validator==2.0.0

# File handling
Pillow==10.0.1

# Date/Time handling
python-dateutil==2.8.2

# MySQL support (if using MySQL)
PyMySQL==1.1.0
cryptography==41.0.4

# PostgreSQL support (if using PostgreSQL)
psycopg2-binary==2.9.7

# Development tools (optional for production)
# python-dotenv==1.0.0

# WSGI server (for local development)
# waitress==2.1.2

# Note: Some packages may need to be installed manually via cPanel Terminal
# if they're not available in the shared hosting environment 